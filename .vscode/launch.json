{
  "configurations": [
    {
      "name": "Play",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build",
      "program": "${env:GODOT_EXECUTABLE}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
